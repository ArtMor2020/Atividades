@model Campeonato.Models.PontosCampeonato

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>PontosCampeonato</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdModalidade" class="control-label">Modalidade</label>
                <select asp-for="IdModalidade" class ="form-control" asp-items="ViewBag.IdModalidade"></select>
            </div>
            <div class="form-group">
                <label asp-for="IdEquipe" class="control-label">Equipe</label>
                <select asp-for="IdEquipe" class ="form-control" asp-items="ViewBag.IdEquipe">
                    <option value="" selected>Selecione uma Equipe</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="IdJogador" class="control-label">Jogador</label>
                <select asp-for="IdJogador" class ="form-control" asp-items="ViewBag.IdJogador">
                    <option value="" selected>Selecione um Jogador</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="Pontos" class="control-label"></label>
                <input asp-for="Pontos" class="form-control" />
                <span asp-validation-for="Pontos" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
